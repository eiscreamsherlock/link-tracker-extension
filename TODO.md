### Fixes

- BETTER VISUALS
- Font size not for ants

### Optimizations

- Add a notes/context field to keep track of why you saved a tab
- Add an export (to CSV?) function?
- Standard box size that scrolls when the list gets "too long"
- More visible icon (draw one?)
- Package as an extension so I can share to folks without their needing to pull from github
